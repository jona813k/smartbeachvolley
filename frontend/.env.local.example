# Vercel Postgres (set in Vercel project settings — auto-injected when linked)
POSTGRES_URL=

# Vercel Blob (set in Vercel project settings — auto-injected when linked)
BLOB_READ_WRITE_TOKEN=

# Admin credentials
ADMIN_USERNAME=jonas
ADMIN_PASSWORD=

# JWT signing secret (openssl rand -base64 32)
JWT_SECRET=
