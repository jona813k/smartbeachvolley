# Neon Postgres (set in Vercel project settings — auto-injected by Neon integration)
POSTGRES_DATABASE_URL=

# Vercel Blob (set in Vercel project settings — auto-injected by Blob integration)
BLOB_READ_WRITE_TOKEN=

# Admin credentials
ADMIN_USERNAME=jonas
ADMIN_PASSWORD=

# JWT signing secret (openssl rand -base64 32)
JWT_SECRET=
