// next.config.mjs — must be .mjs, NOT .ts (Next.js 14 does not support next.config.ts)
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

export default nextConfig
